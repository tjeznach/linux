# SPDX-License-Identifier: GPL-2.0-only
# RISC-V IOMMU support

config RISCV_IOMMU
	bool "RISC-V IOMMU driver"
	depends on RISCV && 64BIT
	select IOMMU_API
	select IOMMU_DMA
	select PCI_ATS
	select PCI_PASID
	select PCI_PRI
	help
	  Support for implementations of the RISC-V IOMMU architecture that
	  complements the RISC-V MMU capabilities, providing similar address
	  translation and protection functions for accesses from I/O devices.

	  Say Y here if your SoC includes an IOMMU device implementing
	  the RISC-V IOMMU architecture.

if RISCV_IOMMU

config RISCV_IOMMU_DEBUGFS
	bool "Export RISC-V IOMMU internals in Debugfs"
	depends on IOMMU_DEBUGFS
	help
	  !!!WARNING!!!

	  DO NOT ENABLE THIS OPTION UNLESS YOU REALLY KNOW WHAT YOU ARE DOING!!!

	  Expose RISC-V IOMMU internals in Debugfs.

	  This option is -NOT- intended for production environments, and should
	  only be enabled for debugging RISC-V IOMMU.

endif # RISCV_IOMMU
